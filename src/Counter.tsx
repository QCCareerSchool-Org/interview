/**
 * Create a React component named `Counter` with two optional props of type
 * `number`: `initialCount` and `increment`.
 * 
 * The component should display the current count and a button.
 * 
 * The current count should initially be the value of `initialCount`. If
 * `initialCount` is not provided, the current count should initially be `0`.
 * 
 * Each time the button is clicked, add the value of `increment` to the
 * current count. If `increment` is not provided, add `1`.
 *
 * The layout and styling are not being judged.
 */